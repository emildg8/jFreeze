import { LoadingBlock } from "@/components/ui/LoadingBlock";

export default function Loading() {
  return (
    <div className="py-12">
      <LoadingBlock label="Загрузка jFreeze…" />
    </div>
  );
}
